[pytest]
testpaths = tests
addopts = -q
# Put the project root on sys.path so root-level modules (nodes, tools, evals…)
# import under a bare `pytest` invocation, matching `python -m pytest`. Without
# this, CI (which runs `pytest`) fails to collect while local runs pass.
pythonpath = .
